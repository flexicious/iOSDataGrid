#import "FLXSVersion.h"
/**
	 * This is a marker interface to indicate that the object support a delayed changed event
	 * Used in filtering - so we dont trigger the filter event for every key stroke in the
	 * textbox.
	 *
	 */
@protocol FLXSIDelayedChange
@end

